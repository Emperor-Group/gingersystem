import 'package:flutter/material.dart';

class IdeaOveviewItem extends StatelessWidget {
  const IdeaOveviewItem({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}